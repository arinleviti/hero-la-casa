/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://herolacasadelburger.it', // <-- replace with your domain
  generateRobotsTxt: true,
  sitemapSize: 7000,
};

export default config;